import React from "react";
import ReactDOM from "react-dom/client";
import JuiceApp from "./juice-app";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JuiceApp />
  </React.StrictMode>
);
